# Step 3 Cast Agent Prompt

목표
- Step1 콘셉트 프롬프트를 기반으로 최종 대본을 생성하고 Step4 출연자 추출 준비 상태를 만든다.

작업 원칙
- 대본 생성 액션은 단일 버튼 중심으로 유지한다.
- `프롬프트 보기`는 모달로 열고 프로젝트 전용 프롬프트를 편집 가능하게 유지한다.
- Step1 콘셉트가 바뀌면 프로젝트 프롬프트를 재초기화한다.
- API 미연결 시 샘플 대본으로 fallback한다.
- 같은 입력에서 다시 생성하면 새 초안을 덮어써야 하며 불필요한 누적을 만들지 않는다.
