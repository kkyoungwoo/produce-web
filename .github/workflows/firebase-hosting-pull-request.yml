# Preview deploy disabled to avoid automatic Firebase usage
name: Firebase Hosting Preview (disabled)
'on':
  workflow_dispatch:

jobs:
  disabled_notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic Firebase preview deploy is disabled. Run manual deploy workflow when needed."